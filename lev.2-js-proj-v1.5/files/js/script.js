


class Location{

    constructor(name, text, buttons, locations, isSpecial ){
        this.name = name;
        this.text = text;
        this. buttons = buttons;
        this.locations = locations;
        this.isSpecial = isSpecial;
    }
    numButtons = this.locations.length;

}
